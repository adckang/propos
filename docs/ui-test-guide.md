# PROPOS UI 검증 문서
> 각 UC 구현 완료 후 이 문서에 누적 추가한다.
> 마지막 업데이트: 2026-04-13

---

## 실행 환경

> 시나리오 정본: `docs/scenarios.yaml`

```bash
cd /Users/kimjinsol/Downloads/propos-project-v2
npm install
npm run dev
# 브라우저: http://localhost:5173
```

### 자동 기준선

```bash
npm run verify:scenarios
npm run test:smoke
```

- `verify:scenarios`: 요구사항 레지스트리, 컴포넌트, 서비스, 다이어그램, 테스트 연결 확인
- `test:smoke`: 랜딩, 시스템 페이지, 시나리오 패널 기본 진입 및 핵심 텍스트 렌더링 확인

---

## UC-001 · D-1 체크인 전날 자동화

**구현 파일:** `src/components/D1AutomationPanel.jsx`
**진입 경로:** 랜딩 → ⚡ D-1 자동화 카드

---

### 사전 조건

| 항목 | 값 |
|------|-----|
| 오류 시뮬레이션 | OFF (기본값) |
| 대상 숙소 수 | 4개 (목업 고정) |
| HA 연동 | 미연동 (Mock 시뮬레이션) |

---

### TC-UI-001 · 진입 및 초기 화면

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | `http://localhost:5173` 접속 | 랜딩 운영 대시보드 표시 |
| 2 | ⚡ **D-1 자동화** 카드 클릭 | D-1 화면 진입 |
| 3 | 화면 확인 | 타이틀: "D-1 체크인 전날 자동화" |
| 4 | 서브타이틀 확인 | `UC-001 · 매일 18:00 · cron: "0 18 * * *"` |
| 5 | 대기 상태 확인 | "내일 체크인 대상: 4개 숙소" + 숙소명 4개 나열 |
| 6 | 버튼 확인 | `▶ 지금 실행` 버튼 활성 상태 |

**Pass 기준:** 6개 항목 모두 충족

---

### TC-UI-002 · 정상 흐름 (전체 성공)

> 오류 시뮬레이션 **OFF** 상태

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | `▶ 지금 실행` 클릭 | 버튼 → "↻ 처리중..." 로 변경 |
| 2 | 처리 중 관찰 | 카드 순차 등장, 처리중 칩 `…` 표시 |
| 3 | 처리 완료 | 카드 4개 모두 `[완료]` 초록 상태 |
| 4 | 각 카드 단계 확인 | ✓PIN 발급 · ✓메시지 · ✓스마트홈 |
| 5 | 요약 바 확인 | 대상 4 / 완료 4 / 부분완료 0 / 실패 0 |
| 6 | 알림 피드 확인 | 우측 카운터 4, info 알림 4개 |
| 7 | 마지막 실행 시각 | 헤더 우측 `마지막 실행 HH:MM` 표시 |

**Pass 기준:** 7개 항목 모두 충족

---

### TC-UI-003 · 오류 흐름 (PIN 등록 실패)

> 오류 시뮬레이션 **ON** — P-007 강남 럭셔리 스위트 실패 시나리오

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 오류 시뮬레이션 토글 클릭 | 토글 빨간색으로 변경 |
| 2 | `▶ 지금 실행` 클릭 | 실행 시작 |
| 3 | P-007 카드 확인 | `[실패]` 빨간 상태 |
| 4 | P-007 단계 확인 | ✕PIN 발급 · 메시지/스마트홈 칩 없음 |
| 5 | P-007 에러 메시지 확인 | "PIN 등록 실패 — HA 503 — 도어락 응답 없음" |
| 6 | 나머지 3개 확인 | 모두 `[완료]` 정상 처리 |
| 7 | 요약 바 확인 | 대상 4 / 완료 3 / 실패 1 |
| 8 | 알림 피드 확인 | error 알림 1개 (빨간) + info 알림 3개 |

**Pass 기준:** 8개 항목 모두 충족

---

### TC-UI-004 · 네비게이션

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | `← 홈` 버튼 클릭 | 랜딩 화면으로 복귀 |
| 2 | 다시 D-1 진입 | 상태 초기화 (결과 카드 없음) |

**Pass 기준:** 2개 항목 모두 충족

---

### TC-UI-005 · 재실행 시 상태 초기화

> 섹션 0 대응 — 매 실행마다 새 결과로 덮어써야 함

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 정상 실행 완료 후 결과 확인 | 카드 4개 완료 상태 |
| 2 | `▶ 지금 실행` 다시 클릭 | 이전 결과 카드 초기화 |
| 3 | 재실행 중 확인 | 카드 처음부터 다시 순차 등장 |
| 4 | 알림 피드 확인 | 이전 알림 초기화, 새 알림만 표시 |

**Pass 기준:** 4개 항목 모두 충족

---

### TC-UI-006 · 처리 순서 검증

> 섹션 1→2→3 순서 대응 — PIN 발급 후 메시지, 그 후 스마트홈 순서여야 함

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 실행 후 첫 번째 카드 처리 중 관찰 | `…PIN` 칩 먼저 등장 |
| 2 | PIN 완료 후 | `✓PIN` → `…메시지` 순으로 전환 |
| 3 | 메시지 완료 후 | `✓메시지` → `…스마트홈` 순으로 전환 |
| 4 | 전체 완료 | `✓PIN ✓메시지 ✓스마트홈` 순서 확인 |

**Pass 기준:** 4개 항목 모두 충족

---

### ⚠️ UI 미구현 항목 (시퀀스 대비 Gap)

> 시퀀스 다이어그램에 명시되어 있으나 현재 UI에서 확인 불가한 항목.
> 향후 구현 시 TC 추가 예정.

| Gap | 시퀀스 섹션 | 설명 | 우선순위 |
|-----|-----------|------|---------|
| 알림 ACK 버튼 | 섹션 4 | `Admin -> AlertFeed : 알림 확인 (ack 클릭)` → 뱃지 해제 기능 없음 | 높음 |
| PIN 유효기간 표시 | 섹션 1 | PIN 발급 시 유효기간(체크인-1h ~ 체크아웃) 카드에 미표시 | 중간 |
| 웰컴 메시지 내용 | 섹션 2 | 게스트명·PIN·WiFi 포함 여부를 UI에서 확인 불가 | 중간 |
| 스마트홈 씬 상세 | 섹션 3 | 조명 100%, 에어컨 26°C 설정값 UI 미표시 | 낮음 |

---

### 최종 체크리스트 UC-001

```
□ TC-UI-001  진입 및 초기 화면
□ TC-UI-002  정상 흐름 — 4개 전부 완료
□ TC-UI-003  오류 흐름 — P-007 실패, 나머지 정상
□ TC-UI-004  네비게이션 — 홈 복귀 후 초기화
□ TC-UI-005  재실행 시 상태 초기화
□ TC-UI-006  처리 순서 — PIN → 메시지 → 스마트홈 순서 확인

⚠️ Gap (미구현):
  - 알림 ACK 버튼 (뱃지 해제)
  - PIN 유효기간 카드 표시
  - 웰컴 메시지 내용 확인
  - 스마트홈 씬 상세값 표시
```

---

## SYS-HA · 홈 어시스턴트

**구현 파일:** `src/components/HomeAssistant.jsx`  
**진입 경로:** 랜딩 → `대표 숙소 상세 보기`

### TC-SYS-HA-001 · 핵심 구조 확인

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 랜딩에서 `대표 숙소 상세 보기` 클릭 | 홈 어시스턴트 진입 |
| 2 | 상단 확인 | 대표 숙소명 표시 |
| 3 | 타임라인 확인 | `D-1 자동화`, `체크인`, `체류 중`, `퇴실·청소`, `정산` 단계 표시 |
| 4 | 포커스 영역 확인 | `지금 할 일` 카드 표시 |
| 5 | 하단 확인 | `빠른 제어` 스트립 표시 |

**Pass 기준:** 5개 항목 모두 충족

### TC-SYS-HA-002 · 단계 전환 확인

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | `D-1 자동화` 단계 클릭 | D-1 관련 카드와 `도어락 PIN 발급` 문구 확인 |
| 2 | `체크인` 단계 클릭 | 체크인 관련 카드와 `체크인 씬 실행 결과` 문구 확인 |
| 3 | `퇴실·청소` 단계 클릭 | 청소 카드와 `청소 체크리스트` 문구 확인 |
| 4 | `정산` 단계 클릭 | 수익 관련 카드와 수익/비용 요약 블록 확인 |

**Pass 기준:** 4개 항목 모두 충족

---

## SYS-CC · 커맨드 센터

**구현 파일:** `src/components/CommandCenter.jsx`  
**진입 경로:** 랜딩 → `커맨드 센터 열기`

### TC-SYS-CC-001 · 초기 관제 구조 확인

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 랜딩에서 `커맨드 센터 열기` 클릭 | 커맨드 센터 진입 |
| 2 | 상단 확인 | `전체 관제 센터` 문구 확인 |
| 3 | 긴급 배너 확인 | `지금 가장 급한 알림` 배너 노출 |
| 4 | 요약 구조 확인 | 파이프라인 카드와 `즉시 대응 큐` 섹션 표시 |
| 5 | 우측 패널 확인 | `실시간 알림` 패널 표시 |

**Pass 기준:** 5개 항목 모두 충족

### TC-SYS-CC-002 · 단계별 탐색 확인

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 상단 파이프라인에서 `센서 모니터링` 클릭 | 해당 단계가 활성 상태로 전환 |
| 2 | 중앙 헤더 확인 | `센서 모니터링에서 지금 먼저 처리할 숙소` 문구 확인 |
| 3 | 즉시 대응 큐 카드 1개 클릭 | 상세 패널에 해당 숙소 정보 표시 |
| 4 | 상세 패널 탭에서 `IoT` 클릭 | `IoT 디바이스` 영역 표시 |

**Pass 기준:** 4개 항목 모두 충족

---

## UC-002 · 체크인 당일 자동화

**구현 파일:** `src/components/S02CheckinPanel.jsx`  
**진입 경로:** 랜딩 → `체크인 당일` 카드

### TC-UI-201 · 초기 화면 구조

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 랜딩에서 `체크인 당일` 카드 클릭 | 체크인 당일 패널 진입 |
| 2 | 헤더 확인 | `UC-002 — 체크인 당일 자동화` 표시 |
| 3 | 포커스 영역 확인 | `지금 확인할 것` 카드 표시 |
| 4 | 좌측 확인 | `숙소 현황` 패널 표시 |
| 5 | 중앙 확인 | `도어락 열림 감지`를 포함한 4단계 흐름 표시 |
| 6 | 우측 확인 | `최근 예외` 카드 또는 알림 영역 표시 |

**Pass 기준:** 6개 항목 모두 충족

### TC-UI-202 · 기본 체크인 자동화 실행

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 상단 또는 STEP 1의 `도어락 열림 이벤트 발생` 버튼 클릭 | 자동화 시작 |
| 2 | 진행 중 확인 | 버튼 또는 단계 카드가 처리 중 상태로 변경 |
| 3 | STEP 2 확인 | 체크인 씬 실행 결과가 반영 |
| 4 | STEP 3 확인 | 채널 오픈 또는 메시지 관련 결과가 표시 |
| 5 | 요약 카드 확인 | 입실 완료/처리 중/대기 중/주의 필요 카운트가 갱신 |

**Pass 기준:** 5개 항목 모두 충족

### TC-UI-203 · 개발 모드 시뮬레이터 노출

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | `시뮬레이터 열기` 클릭 | 개발 모드 패널 펼침 |
| 2 | 패널 내용 확인 | 테스트 숙소, 이벤트 context, 시각 입력 필드 표시 |
| 3 | 오류 토글 확인 | `씬 실패`, `채널 실패` 토글 노출 |
| 4 | 실 HA 전환 확인 | `Mock 모드` / `실제 HA 모드` 토글 노출 |

**Pass 기준:** 4개 항목 모두 충족

---

## UC-003 · 체류 중 실시간 모니터링

**구현 파일:** `src/components/S03MonitoringPanel.jsx`  
**진입 경로:** 랜딩 → `체류 중 모니터링` 카드

### TC-UI-301 · 초기 화면 구조

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 랜딩에서 `체류 중 모니터링` 카드 클릭 | 모니터링 패널 진입 |
| 2 | 헤더 확인 | `UC-003 — 체류 중 실시간 모니터링` 표시 |
| 3 | 포커스 영역 확인 | `지금 먼저 볼 것` 카드 표시 |
| 4 | 좌측 확인 | 모니터링 중 숙소 목록 표시 |
| 5 | 상단 제어 확인 | `▶ 폴링 시작` 버튼과 폴링 간격 선택기 표시 |

**Pass 기준:** 5개 항목 모두 충족

### TC-UI-302 · 폴링 시작과 상태 갱신

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | `▶ 폴링 시작` 클릭 | 버튼이 `■ 폴링 중지`로 변경 |
| 2 | 상태 확인 | 헤더에 `폴링 중` 및 카운트다운 표시 |
| 3 | 포커스 카드 확인 | 마지막 폴링 또는 이상 숙소 정보가 갱신 |
| 4 | `현재 즉시 재폴링` 클릭 | 즉시 재조회 동작이 실행되고 화면이 유지됨 |

**Pass 기준:** 4개 항목 모두 충족

### TC-UI-303 · 숙소 상세 탐색

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 좌측 숙소 목록에서 임의 숙소 클릭 | 선택 숙소 변경 |
| 2 | 우측 상태 카드 확인 | 선택 숙소명과 초안/네트워크 상태 갱신 |
| 3 | 포커스 카드의 `우선 숙소로 이동` 클릭 | 긴급 또는 경고 대상 숙소로 선택 전환 |

**Pass 기준:** 3개 항목 모두 충족

---

## UC-004 · 체크아웃 및 청소 자동화

**구현 파일:** `src/components/S04CheckoutPanel.jsx`  
**진입 경로:** 랜딩 → `체크아웃 & 청소` 카드

### TC-UI-401 · 초기 화면 구조

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 랜딩에서 `체크아웃 & 청소` 카드 클릭 | 체크아웃/청소 패널 진입 |
| 2 | 헤더 확인 | `퇴실 후 정리 현황` 표시 |
| 3 | 포커스 영역 확인 | `지금 먼저 처리할 것` 카드 표시 |
| 4 | 요약 카드 확인 | `퇴실 대기`, `청소 진행`, `청소 배정`, `준비 완료` 카드 표시 |
| 5 | 본문 확인 | `퇴실 감지`, `도어락 PIN 즉시 만료`, `청소팀 자동 배정`, `청소 체크리스트` 단계 표시 |

**Pass 기준:** 5개 항목 모두 충족

### TC-UI-402 · 기본 체크아웃 흐름 실행

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 선택 숙소가 `입실 중` 상태인지 확인 | STEP 1 대기 상태 확인 |
| 2 | `퇴실 이벤트 바로 확인` 또는 `🔒 도어락 잠금 (퇴실 감지)` 클릭 | 자동화 시작 |
| 3 | STEP 2 확인 | PIN 만료 카운트다운 또는 완료 상태 표시 |
| 4 | STEP 3 확인 | 청소팀이 자동 배정되고 도착 예상 시각 표시 |
| 5 | STEP 4 확인 | 청소 체크리스트가 생성됨 |

**Pass 기준:** 5개 항목 모두 충족

### TC-UI-403 · 청소 완료 처리

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 생성된 체크리스트의 필수 항목을 순서대로 클릭 | 완료 항목이 체크 상태로 변경 |
| 2 | 모든 필수 항목 완료 후 하단 버튼 확인 | `청소 완료 — 숙소 준비 완료 처리` 버튼 활성화 |
| 3 | 버튼 클릭 | 숙소 상태가 `준비 완료`로 변경 |
| 4 | 상단 요약 카드 확인 | `준비 완료` 카운트가 증가 |

**Pass 기준:** 4개 항목 모두 충족

---

## UC-005 · 수익 정산 자동화

**구현 파일:** `src/components/S05RevenuePanel.jsx`  
**진입 경로:** 랜딩 → `수익 정산` 카드

### TC-UI-501 · 초기 화면 구조

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 랜딩에서 `수익 정산` 카드 클릭 | 정산 패널 진입 |
| 2 | 헤더 확인 | `이번 달 수익 정산` 표시 |
| 3 | 포커스 영역 확인 | `이번 달 정산에서 먼저 볼 것` 카드 표시 |
| 4 | 좌측 확인 | `정산 설정`, `진행 상태` 패널 표시 |
| 5 | 중앙 확인 | `플랫폼별 수익`, `AI 가격 최적화` 섹션 표시 |
| 6 | 우측 확인 | `세금 리포트`, `자동화 알림` 영역 표시 |

**Pass 기준:** 6개 항목 모두 충족

### TC-UI-502 · 기본 정산 실행

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | `▶ n월 정산 실행` 버튼 클릭 | 버튼이 처리 중 상태로 변경 |
| 2 | 진행 상태 확인 | `정산 시작 → 수익 수집 → AI 최적화 → 세금 리포트` 단계가 순차 반영 |
| 3 | 중앙 확인 | 에어비앤비/야놀자 수익 카드와 `수익 집계 요약` 표시 |
| 4 | AI 섹션 확인 | 추천 리스트와 적용 대상 수가 표시 |
| 5 | 우측 확인 | 세금 리포트와 자동화 알림이 생성 |

**Pass 기준:** 5개 항목 모두 충족

### TC-UI-503 · 추천 적용 및 오류 시뮬레이션

| 순서 | 액션 | 기대 결과 |
|------|------|----------|
| 1 | 정산 완료 후 추천 리스트에서 일부 숙소 선택/해제 | 선택 추천 수가 즉시 반영 |
| 2 | 상단 `선택 추천 바로 적용` 또는 리스트의 적용 버튼 클릭 | 적용 진행 후 알림 생성 |
| 3 | `API 오류 시뮬 ON` 활성화 후 정산 재실행 | 오류 알림이 생성되고 포커스 카드가 예외 중심으로 변경 |

**Pass 기준:** 3개 항목 모두 충족
